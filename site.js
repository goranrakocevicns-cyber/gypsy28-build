function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
let currentLang=localStorage.getItem('gypsy-lang')||'sr';
function setLang(lang){currentLang=lang;localStorage.setItem('gypsy-lang',lang);document.documentElement.lang=lang;document.querySelectorAll('[data-sr][data-en]').forEach(el=>{el.textContent=el.dataset[lang]||el.dataset.sr});document.querySelectorAll('.lang-switch button').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));document.title=lang==='en'?'Gypsy 28 Build — Build Journal':'Gypsy 28 Build — Dnevnik izgradnje';loadPosts();}
document.querySelectorAll('.lang-switch button').forEach(b=>b.addEventListener('click',()=>setLang(b.dataset.lang)));
async function loadPosts(){const box=document.getElementById('posts');if(!box)return;try{const r=await fetch('/content/posts/index.json',{cache:'no-store'});if(!r.ok)return;const posts=await r.json();if(!posts.length)return;box.innerHTML=posts.map(p=>{const en=currentLang==='en',title=en?(p.title_en||p.title):p.title,category=en?(p.category_en||p.category):p.category,summary=en?(p.summary_en||p.summary):p.summary,url=`/post.html?post=${encodeURIComponent(p.slug)}`;return `<article class="card post-card">${p.image?`<a href="${url}"><img src="${esc(p.image)}" alt="${esc(title)}" loading="lazy"></a>`:''}<span>${esc(category||(en?'JOURNAL':'DNEVNIK'))}</span><h3><a href="${url}">${esc(title)}</a></h3><p>${esc(summary||'')}</p><a class="read-more" href="${url}">${en?'Read more →':'Прочитај више →'}</a></article>`}).join('')}catch(e){console.error(e)}}
setLang(currentLang);
